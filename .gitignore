# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
bin/*
Dockerfile.cross

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Go workspace file
go.work

# Do not commit a vendor/ tree; dependencies come from the module proxy + go.mod replaces.
/vendor/

# editor and IDE paraphernalia
.idea
.vscode
*.swp
*.swo
*~
*.cov

# Local git worktrees
.worktrees/

# Built operator binary (local artifact)
/volume-recovery-operator
